# Python
backend/.venv/
__pycache__/
*.pyc
backend/.env
backend/data_1c/
backend/.cache/
template/
# Node / Vite
frontend/node_modules/
frontend/dist/

# OS / editor
.DS_Store

# Датасеты (в т.ч. сгенерированные ML-таблицы) коммитятся целиком,
# чтобы жюри могло проверить данные без запуска пайплайна.
# Spreadsheet editor locks are not source data.
.~lock.*#
~$*.xlsx
HackAlem AI_ Автоматизация формирования заказов поставщикам.docx.pdf

# Критерии оценки — только локально, в GitHub-репозиторий не попадают
критерии/

# Local browser screenshots and generated verification workbooks
qa/artifacts/
